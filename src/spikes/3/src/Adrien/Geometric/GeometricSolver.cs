using System;
using Adrien.Ast;

namespace Adrien.Geometric
{
    /// <summary>
    /// Intended for geometric inference over a compute graph.
    /// </summary>
    public class GeometricSolver
    {
        /// <summary>
        /// The solution of the geometric inference is injected
        /// directly into the variables and indices of the graph.
        /// </summary>
        public void Solve(Graph graph)
        {
            throw new NotImplementedException();
        }
    }
}
