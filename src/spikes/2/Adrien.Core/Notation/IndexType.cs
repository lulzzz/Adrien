namespace Adrien.Notation
{
    public enum IndexType
    {
        Dimension = 1,
        DimensionExpression = 2,
        Literal = 3 
        // TODO: [vermorel] either define the values for all or none.
        // REMARK: [allisterb] All values defined
    }
}